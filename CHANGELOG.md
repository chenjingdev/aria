# Changelog

## v0.1.0-alpha.2 — 2026-09-06

- 시작 안내의 기본 연결 흐름을 **연결 요청 복사 → 이 Mac의 로컬 AI에 붙여넣기**로 바꿨습니다. 현재 설치 경로·등록 방법·기존 설정 보존·실제 MCP 호출 확인을 요청에 포함합니다. 설정만 끝났거나 새 대화가 필요하면 연결 완료와 구분하도록 안내합니다.
- 터미널 명령과 JSON 설정은 **직접 연결하기** 안으로 접었습니다.
- 음원 설치 상태와 안내 완료 기록을 분리했습니다. **안내 마치기**를 명시적으로 누르기 전에는 음원이 있어도 자동 안내를 표시합니다. ×는 이번 창만 닫습니다. 완료 기록은 앱 데이터 폴더에 저장해 재시작·브라우저 변경 후에도 유지합니다.
- 기존 공개 태그는 유지합니다. 기본 MCP 37개와 실험 기능의 범위는 alpha.1과 같습니다.
- 전체 18개 테스트 스크립트와 확장한 첫 사용 통합 검사 34개가 통과했습니다. 브라우저에서는 연결 요청의 정확한 복사, ×로 닫은 뒤 다시 표시, 명시적 완료 후 숨김과 수동 재열기를 확인했습니다. 에이전트별 로그인·새 세션 처리는 각 AI 앱의 동작을 따릅니다.

## v0.1.0-alpha.1 — 2026-09-05

첫 공개 버전은 **macOS에서 외부 AI로 곡을 만들고, 듣고, 보관하고, WAV로 내보내는 프리뷰**입니다. Node.js와 MCP 지원 AI를 사용하는 사람이 기본 음원을 한 번 준비한 뒤 시작하는 소스 설치 방식입니다.

### 첫 사용

- `npm start`가 Mac 브라우저를 열고, 음원이 없으면 기본 음원 설치를 안내합니다.
- GeneralUser 공식 페이지에서 받은 SF2를 Mac 파일 선택창으로 가져옵니다. 검사·설치·악기 목록 갱신·네 음 피아노 확인을 연결했습니다. 취소·잘못된 파일은 설치하지 않고 기존 음원은 보존합니다.
- 시작 안내에 현재 설치 경로를 반영한 Codex/Claude 등록 명령, 다른 MCP 클라이언트용 설정, 연결 확인 요청과 첫 곡 요청을 제공합니다.
- AI 연결 상태는 실제 인증된 도구 요청을 받은 시각으로 표시합니다. 곡이 없는 첫 실행의 `get_song`도 연결 확인에 성공합니다.
- 미설치 음원 파일 수천 개를 나열하던 시작 로그를 요약했고, `pack:install`이 현재 압축 원본 설치기로 연결되도록 고쳤습니다.

### 공개 범위

- 기본 MCP 프로필은 벤치 당시 노출한 범위와 같은 37개입니다. `ARIA_TOOL_PROFILE=full`을 명시하면 45개 전체를 사용할 수 있습니다. `ARIA_HIDE_TOOLS`는 프로필에서 추가로 숨깁니다.
- 기본 MCP에서 제외한 도구: `list_feedback`, `resolve_feedback`, `add_feedback`, `ab_save`, `ab_load`, `list_songs`, `load_song`, `import_midi`. GUI의 곡 보관·불러오기는 계속 제공합니다.
- 세부 노트·강약·마디 직접 편집, A/B와 로컬 메모 저장은 실험 기능으로 기본 숨김입니다. 시작 안내에서 현재 탭에만 켤 수 있습니다.
- 구간 요청은 복사해서 외부 AI에 직접 전달합니다. 로컬 메모 저장만으로 AI가 실행되지는 않습니다.
- 악기 계층 탐색, MP3·Mac 저장 폴더 선택, 브리지 도구 숨김과 세션별 데이터 분리 등 기존 로컬 개선도 이 태그에 포함합니다.

### 검증과 한계

- 전체 18개 자동 테스트 스크립트 통과. 새 설치 통합 검사 24개는 별도 앱 프로세스·실제 stdio MCP로 설치, 기본 음원 작곡, 저장, 비무음 WAV, 재시작 복원을 검증합니다. 파일 선택 결과는 이 통합 검사에서 테스트 음원으로 대체합니다.
- UI/UX 검토: 음원 없는 첫 화면, AI 등록 명령 복사, 실제 도구 요청 표시, 실험 편집 off/on, 구간 요청 복사, 1440px·390px 화면과 콘솔 오류 없음 확인.
- Windows amd에서는 공식 GeneralUser를 사용한 설치·제거 2회를 확인했지만 Mac 파일 선택·일반 곡 재생을 지원하는 결과로 보지 않습니다.
- AI·계정·기본 음원은 별도 준비합니다. MP3에는 FFmpeg가 필요하고, 추가 SFZ 팩에는 로컬 엔진 빌드가 필요합니다. 기본 SF2 곡과 WAV에는 이 추가 준비가 필요하지 않습니다.
- 새 물리 Mac에서 처음 보는 사람이 공식 다운로드·OS 파일 선택·AI 로그인까지 끝내는 사용자 테스트와 대형 팩 전체 재설치 검증은 남아 있습니다.
- 벤치에서 실제 호출된 도구는 23종입니다. 37종 전체의 모든 편집 조합을 벤치가 검증했다는 뜻은 아닙니다. [기능 범위와 증거](docs/RELEASE_SCOPE.md)

### 설치·업데이트

[README](README.md#처음-설치하기)의 설치 안내를 따르세요. 실행 중인 앱을 종료한 뒤 `main`에서 `git pull --ff-only`, `npm ci`, `npm start`를 실행하면 이 공개 버전을 받습니다. 정확히 고정하려면 `v0.1.0-alpha.1` 태그를 선택하세요. 곡과 음원은 코드 폴더 밖의 `~/.aria`에 유지됩니다.
